title: "Collections Test Site"
baseURL: "https://example.com"

build:
  output: "_site"

taxonomies:
  tags:
    permalink: "/tags/:slug/"
    layout: "tags"
  categories:
    permalink: "/categories/:slug/"
    layout: "categories"

collections:
  blog:
    sortBy: "date"
    order: "desc"
