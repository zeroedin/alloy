---
type: patch
---

Support disabling sitemap generation site-wide with `sitemap: false` in the config file. The object form (`sitemap: {changefreq: ..., priority: ...}`) continues to work as before. When omitted, sitemaps are generated by default.
