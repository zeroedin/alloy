[build]
  command = "npm ci && go build -o alloy . && ./alloy build"
  publish = "_site"

[build.environment]
  GO_VERSION = "1.25.4"

[context.deploy-preview]
  command = "npm ci && go build -o alloy . && ./alloy build"

[context.branch-deploy]
  command = "npm ci && go build -o alloy . && ./alloy build"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
